# ResNet-based downscaling model configuration
#
# Consumed by hydra.utils.instantiate (train.py) or by build_model().
# Switch to this config with:   python train.py model=resnet

_target_: src.models.resnet.ResNetDownscaler

in_channels: 1
out_channels: 1
num_residual_blocks: 8
num_features: 64
scale_factor: 4
